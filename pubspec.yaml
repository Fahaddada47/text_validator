name: text_validator
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/fahadislam-dev/text_validator
repository: https://github.com/fahadislam-dev/text_validator


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - formvalidation

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

flutter:
  uses-material-design: true
